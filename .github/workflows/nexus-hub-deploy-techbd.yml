name: Nexus Deployment
on:
  push:
    tags:
      - '*'
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
    nexus-hub-sandbox-deployment:
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-hub-sandbox-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/nexus-hub.sandbox.techbd.org/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-hub-ui sandbox ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-hub-sandbox-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-hub-sandbox-deployment-${{ github.ref_name}}-deployment --title "Nexus sandbox techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus sandbox deployment for version ${{ github.ref_name }}"

    nexus-api-sandbox-deployment:
      needs: nexus-hub-sandbox-deployment
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-api-sandbox-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/nexus-ingestion-api-sbx/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-api-sandbox ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-api-sandbox-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-api-sandbox-deployment-${{ github.ref_name}}-deployment --title "Nexus sandbox API techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus sandbox API deployment for version ${{ github.ref_name }}"

    nexus-fhir-sandbox-deployment:
      needs: nexus-hub-sandbox-deployment
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-fhir-sandbox-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/nexus-fhir.sandbox.interface.engine.techbd.org/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-fhir-sandbox ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-fhir-sandbox-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-fhir-sandbox-deployment-${{ github.ref_name}}-deployment --title "Nexus sandbox FHIR techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus sandbox FHIR deployment for version ${{ github.ref_name }}"


    nexus-csv-sandbox-deployment:
      needs: nexus-hub-sandbox-deployment
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-csv-sandbox-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/nexus-csv.sandbox.interface.engine.techbd.org/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-csv-sandbox ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-csv-sandbox-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-csv-sandbox-deployment-${{ github.ref_name}}-deployment --title "Nexus sandbox CSV techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus sandbox CSV deployment for version ${{ github.ref_name }}"

    nexus-hub-devl-deployment:
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-hub-devl-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/hub.dev.techbd.org/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-hub-ui devl ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-hub-devl-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-hub-devl-deployment-${{ github.ref_name}}-deployment --title "Nexus devl techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus devl deployment for version ${{ github.ref_name }}"

    nexus-fhir-devl-deployment:
      needs: nexus-hub-devl-deployment
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-fhir-devl-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/nexus-fhir.dev.techbd.org/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-fhir-devl ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-fhir-devl-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-fhir-devl-deployment-${{ github.ref_name}}-deployment --title "Nexus devl FHIR techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus devl FHIR deployment for version ${{ github.ref_name }}"

    nexus-csv-devl-deployment:
      needs: nexus-hub-devl-deployment
      if: endsWith(github.ref, '-nexus')  # Only run if tag ends with '-nexus'
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

        - name: Configure Git
          run: |
            git config --global user.email "action@github.com"
            git config --global user.name "GitHub Action"

        - name: Clone infrastructure repository
          run: |
            git clone https://${{ secrets.ACTIONS_GITHUB_TOKEN }}@github.com/tech-by-design/infrastructure-prime.git

        - name: Create and switch to a new branch
          run: |
            cd infrastructure-prime
            git checkout -b nexus-csv-devl-deployment-${{ github.ref_name}}-deployment

        - name: Update fhir environment file
          run: |
            cd infrastructure-prime
            sed -i 's/TAG=.*/TAG=${{ github.ref_name }}/g' aws/nexus-csv.dev.techbd.org/.env

        - name: Commit and push changes
          run: |
            cd infrastructure-prime
            git add .
            git commit -m "ci: nexus-csv-devl ${{ github.ref_name }} deployment"
            git push --set-upstream origin nexus-csv-devl-deployment-${{ github.ref_name}}-deployment
        - name: Create Pull Request
          env:
            GH_TOKEN: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          run: |
            cd infrastructure-prime
            gh pr create --base main --head nexus-csv-devl-deployment-${{ github.ref_name}}-deployment --title "Nexus devl CSV techbd.org ${{ github.ref_name }} update" --body "Automated techbd Nexus devl CSV deployment for version ${{ github.ref_name }}"
